'use server';
import ResetPasswordForm from '@/components/organisms/forms/ResetPasswordForm';

const ResetPasswordPage = async (): Promise<React.ReactElement> => {
  return <ResetPasswordForm />;
};

export default ResetPasswordPage;
